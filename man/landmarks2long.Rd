% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landmarks2long.R
\name{landmarks2long}
\alias{landmarks2long}
\title{Convert wide form landmark table into long form boundaries or durations}
\usage{
landmarks2long(landmarks, form = "boundaries", id = NULL)
}
\arguments{
\item{landmarks}{Either a wide form dataframe or a vector.
If a vector, it is considered a landmark sequence for a single curve.}

\item{form}{Either "boundaries" or "duration", depending on the desired output form.}

\item{id}{Column name of \code{landmarks} that contains curve index, if present.}
}
\value{
A long format dataframe of interval boundaries or durations.
}
\description{
Given a wide format dataframe listing landmark positions, one row per curve,
convert into a long format dataframe of either boundary pairs (from, to) or interval durations.
}
\examples{
library(tibble)
land <- tribble(
~curveId, ~l1, ~l2, ~l3, ~l4,
1, 0, 0.7, 1, 1.5,
2, 0, 0.5, 1.1, 1.8,
3, 0, 0.6, 1.2, 2
)
landmarks2long(land, "curveId")
}
